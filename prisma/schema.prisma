generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id      Int @id @default(autoincrement())
  role_id Int

  nama     String
  email    String
  password String
  nomor_hp String
  profile  String? @db.VarChar(250)
  gender   String  @default("Male") @db.VarChar(50)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  role Roles @relation(fields: [role_id], references: [id])

  Pemesanan         Pemesanan[]
  Pemesanan_package PemesananPackage[]
  AdminWa           AdminWa[]
}

model OurPartner {
  id        Int    @id @default(autoincrement())
  nama      String @db.VarChar(250)
  url       String @db.VarChar(250)
  nama_file String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

model Testimonial {
  id           Int     @id @default(autoincrement())
  pemesanan_id Int?
  nama         String  @db.VarChar(250)
  pekerjaan    String  @db.VarChar(250)
  deskripsi    String  @db.LongText
  is_active    Boolean @default(true)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  pemesanan Pemesanan? @relation(fields: [pemesanan_id], references: [id])
}

model Kendaraan {
  id        Int  @id @default(autoincrement())
  lokasi_id Int?
  driver_id Int?

  nama                   String    @db.VarChar(250)
  model                  String?   @db.VarChar(250)
  tipe                   String    @default("MOBIL") @db.VarChar(250)
  plat_nomor             String    @db.VarChar(250)
  kapasitas              String?   @db.VarChar(250)
  tanggal_pajak_berakhir DateTime? @db.DateTime()
  harga                  Decimal   @default(0)

  status_pajak   Boolean @default(true)
  status         Boolean @default(true)
  top_attraction Boolean @default(true)
  is_popular     Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  lokasi Lokasi? @relation(fields: [lokasi_id], references: [id])
  driver Driver? @relation(fields: [driver_id], references: [id])

  kendaraan_content KendaraanContent[]
  kendaraan_file    KendaaranFile[]
  kendaraan_durasi  KendaraanDurasi[]
}

model KendaraanContent {
  id           Int @id @default(autoincrement())
  kendaraan_id Int

  bahasa    String  @default("INDONESIA") @db.VarChar(250)
  deskripsi String? @db.LongText
  kebijakan String? @db.LongText
  informasi String? @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  kendaraan Kendaraan @relation(fields: [kendaraan_id], references: [id])
}

model KendaraanDurasi {
  id           Int @id @default(autoincrement())
  kendaraan_id Int

  durasi String @db.VarChar(250)

  harga         Decimal
  createdAt     DateTime        @default(now())
  updatedAt     DateTime?
  kendaraan     Kendaraan       @relation(fields: [kendaraan_id], references: [id])
  PemesananItem PemesananItem[]
}

model KendaaranFile {
  id           Int @id @default(autoincrement())
  kendaraan_id Int

  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  kendaraan Kendaraan @relation(fields: [kendaraan_id], references: [id])
}

model Driver {
  id Int @id @default(autoincrement())

  nama      String  @db.VarChar(250)
  driver_id String? @db.VarChar(250)
  nomor_hp  String  @db.VarChar(50)
  gender    String  @default("Male") @db.VarChar(50)

  fluent_english Boolean @default(false)

  tanggal_periode_berakhir DateTime? @db.DateTime()

  createdAt DateTime    @default(now())
  updatedAt DateTime?
  Kendaraan Kendaraan[]
}

model Guide {
  id Int @id @default(autoincrement())

  nama      String  @db.VarChar(250)
  driver_id String? @db.VarChar(250)
  nomor_hp  String  @db.VarChar(50)
  gender    String  @default("Male") @db.VarChar(50)

  tanggal_periode_berakhir DateTime? @db.DateTime()

  fluent_english Boolean @default(false)

  createdAt     DateTime        @default(now())
  updatedAt     DateTime?
  TravelPackage TravelPackage[]
}

model Lokasi {
  id        Int @id @default(autoincrement())
  negara_id Int

  nama   String  @db.VarChar(250)
  alamat String? @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  status    Boolean   @default(true)

  negara Negara @relation(fields: [negara_id], references: [id])

  travel_package TravelPackage[]
  akomodasi      Akomodasi[]
  kendaraan      Kendaraan[]
  Blog           Blog[]
}

//TODO: DONE WITH ERROR IN UPDATE AND GET DETAILS DELETE
model Negara {
  id Int @id @default(autoincrement())

  nama      String  @db.VarChar(250)
  kode      String  @db.VarChar(250)
  nama_file String? @db.VarChar(250)
  url       String? @db.VarChar(250)
  status    Boolean @default(true)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  Kota      Kota[]
  Lokasi    Lokasi[]
}

model Kota {
  id        Int     @id @default(autoincrement())
  negara_id Int
  nama      String  @db.VarChar(250)
  alamat    String? @db.LongText
  status    Boolean @default(true)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  negara Negara @relation(fields: [negara_id], references: [id])
}

//DONE
model Pemesanan {
  id      Int @id @default(autoincrement())
  user_id Int

  total_harga Decimal @default(0)
  status      String  @db.VarChar(250)
  type        String? @db.VarChar(50)

  deskripsi String? @db.LongText
  notes     String? @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  user User @relation(fields: [user_id], references: [id])

  pembayaran     Pembayaran[]
  notifikasi_wa  NotfikasiWa[]
  pemesanan_item PemesananItem[]
  Testimonial    Testimonial[]
  Notification   Notification[]
}

model HeroSection {
  id Int @id @default(autoincrement())

  title     String @db.VarChar(250)
  url       String @db.VarChar(250)
  nama_file String @db.VarChar(250)

  status Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

model ServiceSchedule {
  id Int @id @default(autoincrement())

  hari     String @db.VarChar(50)
  jam_buka String @db.VarChar(100)

  is_special_day Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

model PrivacyAndPolicy {
  id Int @id @default(autoincrement())

  title_id         String @db.VarChar(250)
  title_en         String @db.VarChar(250)
  content_id       String @db.LongText
  content_en       String @db.LongText
  contact_title_id String @db.VarChar(250)
  contact_title_en String @db.VarChar(250)
  contact_email    String @db.VarChar(250)
  contact_phone    String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

model TermsAndCondition {
  id Int @id @default(autoincrement())

  title_id   String @db.VarChar(250)
  title_en   String @db.VarChar(250)
  content_id String @db.LongText
  content_en String @db.LongText

  is_active Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

model AboutUs {
  id Int @id @default(autoincrement())

  title_en   String @db.VarChar(250)
  title_id   String @db.VarChar(250)
  content_en String @db.LongText
  content_id String @db.LongText

  createdAt           DateTime              @default(now())
  updatedAt           DateTime?
  is_published        Boolean               @default(true)
  AboutUsStory        AboutUsStory?
  AboutUsServices     AboutUsServices[]
  AboutUsAchievements AboutUsAchievements[]
  AboutUsFile         AboutUsFile[]
  AboutUsOperational  AboutUsOperational[]
  AboutUsCta          AboutUsCta?
}

model AboutUsCta {
  id          Int @id @default(autoincrement())
  about_us_id Int @unique

  title_en       String @db.VarChar(250)
  title_id       String @db.VarChar(250)
  description_en String @db.LongText
  description_id String @db.LongText

  button_text String @db.VarChar(250)
  button_url  String @db.VarChar(250)

  cta_button_text String? @db.VarChar(250)
  cta_button_url  String? @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  about_us  AboutUs   @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model AboutUsOperational {
  id          Int @id @default(autoincrement())
  about_us_id Int

  day  String @db.VarChar(50)
  time String @db.VarChar(100)

  is_highlight Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  about_us  AboutUs   @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model AboutUsStory {
  id          Int @id @default(autoincrement())
  about_us_id Int @unique

  title_en   String @db.VarChar(250)
  title_id   String @db.VarChar(250)
  content_en String @db.LongText
  content_id String @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  about_us AboutUs @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model AboutUsServices {
  id          Int @id @default(autoincrement())
  about_us_id Int

  title_en   String @db.VarChar(250)
  title_id   String @db.VarChar(250)
  content_en String @db.LongText
  content_id String @db.LongText

  icon     String? @db.VarChar(250)
  location String? @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  about_us  AboutUs   @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model AboutUsAchievements {
  id Int @id @default(autoincrement())

  number     String @db.VarChar(250)
  content_en String @db.LongText
  content_id String @db.LongText

  icon String? @db.VarChar(250)

  createdAt   DateTime  @default(now())
  updatedAt   DateTime?
  about_us_id Int
  about_us    AboutUs   @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model AboutUsFile {
  id          Int @id @default(autoincrement())
  about_us_id Int

  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  about_us  AboutUs   @relation(fields: [about_us_id], references: [id], onDelete: Cascade)
}

model PemesananItem {
  id           Int  @id @default(autoincrement())
  pemesanan_id Int
  item_id      Int
  durasi_id    Int?
  room_id      Int?

  jumlah_dewasa Int?
  jumlah_anak   Int?

  durasi_hari Int?

  is_priority Boolean @default(false)

  item_type String @db.VarChar(50)

  total_harga Decimal @default(0)

  tanggal_mulai   DateTime  @default(now())
  tanggal_selesai DateTime? @db.DateTime()

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  Pemesanan Pemesanan              @relation(fields: [pemesanan_id], references: [id])
  durasi    KendaraanDurasi?       @relation(fields: [durasi_id], references: [id])
  room      AkomodasiRoomAndPrice? @relation(fields: [room_id], references: [id])

  @@index([item_type, item_id])
}

//DONE
model Pembayaran {
  id           Int     @id @default(autoincrement())
  pemesanan_id Int
  invoice_id   String? @db.VarChar(250)

  tanggal_bayar DateTime @default(now())
  invoice_url   String?  @db.VarChar(250)
  jumlah_bayar  Decimal  @default(0)
  status        String   @db.VarChar(250)
  metode        String?  @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  pemesanan      Pemesanan         @relation(fields: [pemesanan_id], references: [id])
  pembayaranFile PemabayaranFile[]
}

model PemabayaranFile {
  id            Int @id @default(autoincrement())
  pembayaran_id Int

  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  pembayaran Pembayaran @relation(fields: [pembayaran_id], references: [id])
}

// TODO: DONE WITH ERROR IN UPDATE
model TravelPackage {
  id          Int  @id @default(autoincrement())
  lokasi_id   Int
  category_id Int?
  guide_id    Int?

  nama        String @db.VarChar(250)
  tipe_durasi String @default("hari") @db.VarChar(250)

  harga_dewasa Decimal @default(0)
  harga_anak   Decimal @default(0)

  durasi String? @default("")

  top_attraction Boolean @default(true)
  is_popular     Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  lokasi   Lokasi    @relation(fields: [lokasi_id], references: [id])
  category Category? @relation(fields: [category_id], references: [id])
  guide    Guide?    @relation(fields: [guide_id], references: [id])

  travel_package_content   TravelPackageContent[]
  pemesananPackage         PemesananPackage[]
  travelPackageFile        TravelPackageFile[]
  travel_package_itinerary TravelPackageItinerary[]
}

model TravelPackageContent {
  id                Int @id @default(autoincrement())
  travel_package_id Int

  bahasa    String  @default("INDONESIA") @db.VarChar(250)
  deskripsi String? @db.LongText
  kebijakan String? @db.LongText
  informasi String? @db.LongText
  itinerary String  @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  travel_package TravelPackage @relation(fields: [travel_package_id], references: [id])
}

model TravelPackageItinerary {
  id                Int @id @default(autoincrement())
  travel_package_id Int

  nama   String @db.VarChar(250)
  bahasa String @default("INDONESIA") @db.VarChar(50)

  deskripsi      String        @db.LongText
  createdAt      DateTime      @default(now())
  updatedAt      DateTime?
  travel_package TravelPackage @relation(fields: [travel_package_id], references: [id])
}

model TravelPackageFile {
  id                Int @id @default(autoincrement())
  travel_package_id Int

  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  travel_package TravelPackage @relation(fields: [travel_package_id], references: [id])
}

model PemesananPackage {
  id                Int @id @default(autoincrement())
  travel_package_id Int
  user_id           Int

  tanggal_berangkat DateTime @default(now())
  jumlah_orang      Int
  total_harga       Decimal  @default(0)
  status            String   @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  user          User          @relation(fields: [user_id], references: [id])
  travelPackage TravelPackage @relation(fields: [travel_package_id], references: [id])
}

model NotfikasiWa {
  id           Int @id @default(autoincrement())
  pemesanan_id Int

  tipe_notifikasi String @default("pemesanan") @db.VarChar(250)
  penerima        String @default("admin") @db.VarChar(250)
  status          String @default("terkirim") @db.VarChar(250)

  pesan       String   @db.LongText
  nomor_wa    String   @db.VarChar(250)
  waktu_kirim DateTime @db.DateTime()

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  pemesanan Pemesanan @relation(fields: [pemesanan_id], references: [id])
}

//DONE
model AdminWa {
  id      Int @id @default(autoincrement())
  user_id Int
  role_id Int

  nomor_wa String @db.VarChar(250)
  nama     String @db.VarChar(250)
  session  String @db.VarChar(250)

  is_active Boolean @default(true)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  role Roles @relation(fields: [role_id], references: [id])
  user User  @relation(fields: [user_id], references: [id])
}

model Akomodasi {
  id        Int @id @default(autoincrement())
  lokasi_id Int

  nama     String @db.VarChar(250)
  kategori String @db.VarChar(250)
  tipe     String @default("hotel") @db.VarChar(250)

  top_attraction Boolean @default(true)
  status         Boolean @default(true)
  is_popular     Boolean @default(false)

  harga Decimal @default(0)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  lokasi                   Lokasi                   @relation(fields: [lokasi_id], references: [id])
  akomodasi_content        AkomodasiContent[]
  akomodasi_file           AkomodasiFile[]
  akomodasi_room_and_price AkomodasiRoomAndPrice[]
  akomodasi_facility_group AkomodasiFacilityGroup[]
}

model AkomodasiRoomAndPrice {
  id           Int       @id @default(autoincrement())
  akomodasi_id Int
  nama         String    @db.VarChar(250)
  harga        Decimal   @default(0)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime?

  akomodasi     Akomodasi       @relation(fields: [akomodasi_id], references: [id], onDelete: Cascade)
  PemesananItem PemesananItem[]
  AkomodasiFile AkomodasiFile[]
}

model Notification {
  id           Int @id @default(autoincrement())
  pemesanan_id Int

  data   String @db.Text
  status String @db.VarChar(250)

  is_read Boolean @default(false)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  pemesanan Pemesanan @relation(fields: [pemesanan_id], references: [id])
}

model AkomodasiFacilityGroup {
  id           Int       @id @default(autoincrement())
  akomodasi_id Int
  nama         String    @db.VarChar(250) // e.g., "Deluxe"
  createdAt    DateTime  @default(now())
  updatedAt    DateTime?

  type Int @default(1)

  akomodasi Akomodasi           @relation(fields: [akomodasi_id], references: [id], onDelete: Cascade)
  fasilitas AkomodasiFacility[]
}

model AkomodasiFacility {
  id                Int       @id @default(autoincrement())
  facility_group_id Int
  nama              String    @db.VarChar(250) // e.g., "TV", "AC"
  createdAt         DateTime  @default(now())
  updatedAt         DateTime?

  group AkomodasiFacilityGroup @relation(fields: [facility_group_id], references: [id], onDelete: Cascade)
}

model AkomodasiContent {
  id           Int @id @default(autoincrement())
  akomodasi_id Int

  bahasa    String  @default("INDONESIA") @db.VarChar(250)
  deskripsi String? @db.LongText
  kebijakan String? @db.LongText
  informasi String? @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  akomodasi Akomodasi @relation(fields: [akomodasi_id], references: [id], onDelete: Cascade)
}

model AkomodasiFile {
  id           Int  @id @default(autoincrement())
  akomodasi_id Int
  room_id      Int?

  type Int @default(1)

  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  akomodasi Akomodasi              @relation(fields: [akomodasi_id], references: [id], onDelete: Cascade)
  room      AkomodasiRoomAndPrice? @relation(fields: [room_id], references: [id], onDelete: Cascade)
}

//DONE
model Roles {
  id Int @id @default(autoincrement())

  name String

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  admin_wa AdminWa[]
  user     User[]
}

model News {
  id          Int @id @default(autoincrement())
  category_id Int

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  news_category NewsCategory  @relation(fields: [category_id], references: [id])
  news_file     NewsFile[]
  news_content  NewsContent[]
}

model NewsContent {
  id      Int @id @default(autoincrement())
  news_id Int

  judul     String @db.VarChar(250)
  bahasa    String @default("INDONESIA") @db.VarChar(250)
  deskripsi String @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  news      News      @relation(fields: [news_id], references: [id])
}

model NewsFile {
  id        Int    @id @default(autoincrement())
  news_id   Int
  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  news News @relation(fields: [news_id], references: [id])
}

model NewsCategory {
  id Int @id @default(autoincrement())

  name String

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  News      News[]
}

model Blog {
  id          Int    @id @default(autoincrement())
  category_id Int?
  lokasi_id   Int
  slug        String @default("") @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  blog_category Category?     @relation(fields: [category_id], references: [id])
  blog_file     BlogFile[]
  blog_content  BlogContent[]

  lokasi Lokasi @relation(fields: [lokasi_id], references: [id])
}

model BlogFile {
  id        Int    @id @default(autoincrement())
  blog_id   Int
  nama_file String @db.VarChar(250)
  url       String @db.VarChar(250)

  createdAt DateTime  @default(now())
  updatedAt DateTime?

  blog Blog @relation(fields: [blog_id], references: [id])
}

model BlogContent {
  id      Int @id @default(autoincrement())
  blog_id Int

  judul     String @db.VarChar(250)
  bahasa    String @default("INDONESIA") @db.VarChar(250)
  deskripsi String @db.LongText

  createdAt DateTime  @default(now())
  updatedAt DateTime?
  blog      Blog      @relation(fields: [blog_id], references: [id])
}

model Category {
  id Int @id @default(autoincrement())

  name String

  createdAt     DateTime        @default(now())
  updatedAt     DateTime?
  TravelPackage TravelPackage[]
  Blog          Blog[]
}

model TemplateMessage {
  id Int @id @default(autoincrement())

  category String @default("notifikasi") @db.VarChar(250)
  name     String @db.VarChar(250)

  status String @default("PENDING")

  text_to_customer String @db.LongText
  text_to_admin    String @db.LongText

  is_active Boolean @default(true)

  createdAt DateTime  @default(now())
  updatedAt DateTime?
}

enum TipeKendaraan {
  mobil
  motor
}

enum Bahasa {
  indonesia
  inggris
}

enum TipeNotifikasi {
  pemesanan
  pembayaran
  selesai
  perjalanan
}

enum PenerimaNotifikasi {
  admin
  customer
}

enum StatusPengiriman {
  terkirim
  gagal
}

model WhatsappChannel {
  id        Int      @id @default(autoincrement())
  userId    Int
  session   String
  status    String   @default("PENDING") // SCAN_QR, CONNECTED, DISCONNECTED
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum TipeAkomodasi {
  hotel
  eco_lodge
  guest_house
}
